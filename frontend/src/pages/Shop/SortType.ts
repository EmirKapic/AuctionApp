enum SortType {
  DEFAULT = "Default sorting",
  DATE_CREATED = "Sort by recent",
  TIME_LEFT = "Sort by time left",
  LOW_TO_HIGH = "Lowest price first",
  HIGH_TO_LOW = "Highest price first",
}

export default SortType;
