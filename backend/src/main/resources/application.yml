spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/auctionappdb
    username: postgres
    password: emir
    driver-class-name: org.postgresql.Driver
